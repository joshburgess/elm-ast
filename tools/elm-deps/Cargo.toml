[package]
name = "elm-deps"
version = "0.1.0"
edition = "2024"
description = "Module dependency graph analyzer for Elm projects"
license = "MIT OR Apache-2.0"

[dependencies]
elm-ast-rs = { path = "../.." }
