[package]
name = "elm-search"
version = "0.1.0"
edition = "2024"
description = "Semantic AST-aware code search for Elm projects"
license = "MIT OR Apache-2.0"

[dependencies]
elm-ast-rs = { path = "../.." }
