[package]
name = "elm-unused"
version = "0.1.0"
edition = "2024"
description = "Fast dead code detector for Elm projects"
license = "MIT OR Apache-2.0"

[dependencies]
elm-ast-rs = { path = "../.." }
